# VM Setup

This directory contains scripts for setting up the VM environment used for ZKP compiler testing.

## Scripts

- **install_podman.sh** — Installs Podman on Ubuntu 20.04 (Focal) via the Kubic repository. Run with:
  ```bash
  bash install_podman.sh
  ```
